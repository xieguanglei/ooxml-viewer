{
  "name": "ooxml-viewer-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm:build": "wasm-pack build ../wasm-core --target web --out-dir ../web/pkg",
    "wasm:build:release": "wasm-pack build ../wasm-core --target web --out-dir ../web/pkg --release",
    "wasm:clean": "rm -rf ./pkg ../wasm-core/pkg",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
